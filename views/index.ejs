<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12 text-center">
            <h1 class="display-4 text-primary"><span data-i18n="welcome">Welcome to</span> <span style="color: #009d4f; font-weight: bold;">Kattapult</span> <span style="font-size: 1.2em;">🐱</span></h1>
            <p class="lead" data-i18n="shareContent">Share content, connect with others, and discover something new.</p>
        </div>
    </div>
    
    <% if (locals.error) { %>
        <div class="col-12 mb-4">
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <span data-i18n="<%= error %>"><%= error %></span>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>
    <% } %>
    
    <% if (locals.success) { %>
        <div class="col-12 mb-4">
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <span data-i18n="<%= success %>"><%= success %></span>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        </div>
    <% } %>

    <div class="row">
        <div class="col-md-6">
            <% items.forEach(function(item, index) { %>
                <% if (index % 2 === 0) { %>
                    <div class="mb-4">
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex align-items-center mb-3">
                                    <% if (item.author_picture) { %>
                                        <img src="<%= item.author_picture %>" alt="Author" class="profile-picture me-2">
                                    <% } else { %>
                                        <div class="profile-picture-placeholder me-2">
                                            <span><%= item.author_display_name ? item.author_display_name.charAt(0).toUpperCase() : 'U' %></span>
                                        </div>
                                    <% } %>
                                    <div>
                                        <h5 class="card-title mb-0"><%= item.title %></h5>
                                        <small class="text-muted"><span data-i18n="postedBy">Posted by</span> <%= item.author_display_name ? `${item.author_display_name} (@${item.author_username})` : item.author_username %> <span data-i18n="postedOn">on</span> <%= new Date(item.created_at).toLocaleString() %></small>
                                    </div>
                                </div>
                                
                                <p class="card-text"><%= item.content %></p>
                                
                                <% if (item.gif_url) { %>
                                    <div class="mb-3">
                                        <img src="<%= item.gif_url %>" alt="GIF" class="img-fluid rounded" style="max-height: 300px;">
                                    </div>
                                <% } %>
                                
                                <div class="d-flex justify-content-between align-items-center mb-3">
                                    <% if (locals.user) { %>
                                        <form action="/vote/<%= item.id %>?type=upvote" method="POST" class="d-inline">
                                            <button type="submit" class="btn btn-sm <%= item.hasVoted ? 'btn-primary' : 'btn-outline-primary' %>">
                                                <i class="bi bi-hand-thumbs-up<%= item.hasVoted ? '-fill' : '' %>"></i>
                                                <% if (item.hasVoted) { %>
                                                    <span data-i18n="unvote">Unvote</span>
                                                <% } else { %>
                                                    <span data-i18n="vote">Vote</span>
                                                <% } %>
                                            </button>
                                        </form>
                                        
                                        <% if (locals.user.id === item.user_id || locals.user.role === 'admin') { %>
                                            <div class="d-flex gap-2">
                                                <button type="button" class="btn btn-sm btn-primary" data-bs-toggle="modal" data-bs-target="#editPostModal<%= item.id %>">
                                                    <span data-i18n="edit">Edit Post</span>
                                                </button>
                                                <form action="/post/delete/<%= item.id %>" method="POST" class="d-inline" onsubmit="return confirm(translate('deleteConfirmation'))">
                                                    <button type="submit" class="btn btn-sm btn-danger"><span data-i18n="deletePost">Delete Post</span></button>
                                                </form>
                                            </div>
                                        <% } %>
                                    <% } %>
                                    <div class="d-flex gap-2">
                                        <span class="badge bg-primary"><span data-i18n="votes">Votes</span>: <%= item.vote_count %></span>
                                        <span class="badge bg-secondary"><span data-i18n="comments">Comments</span>: <%= item.comments.length %></span>
                                    </div>
                                </div>
                                
                                <% if (item.comments.length > 0) { %>
                                    <div class="mt-3">
                                        <button class="btn btn-sm btn-outline-secondary mb-2 toggle-comments-btn" data-item-id="<%= item.id %>">
                                            <i class="bi bi-chat-square-text"></i> <span data-i18n="showHideComments">Show/Hide Comments</span> (<%= item.comments.length %>)
                                        </button>
                                        <div class="comments-container" data-item-id="<%= item.id %>">
                                            <h6 class="text-muted mb-2"><span data-i18n="comments">Comments</span>:</h6>
                                            <% item.comments.forEach(comment => { %>
                                                <div class="card comment-card mb-2">
                                                    <div class="card-body">
                                                        <div class="d-flex justify-content-between align-items-start">
                                                            <div>
                                                                <div class="d-flex align-items-center mb-2">
                                                                    <% if (comment.commenter_picture) { %>
                                                                        <img src="<%= comment.commenter_picture %>" alt="Commenter" class="profile-picture profile-picture-sm me-2">
                                                                    <% } else { %>
                                                                        <div class="profile-picture-placeholder profile-picture-sm me-2">
                                                                            <span><%= comment.commenter_display_name ? comment.commenter_display_name.charAt(0).toUpperCase() : 'U' %></span>
                                                                        </div>
                                                                    <% } %>
                                                                    <h6 class="card-subtitle mb-1 text-muted">
                                                                        <%= comment.commenter_display_name ? `${comment.commenter_display_name} (@${comment.commenter_username})` : comment.commenter_username %>
                                                                    </h6>
                                                                </div>
                                                                <% if (comment.content && comment.content.trim() !== '') { %>
                                                                    <p class="card-text"><%= comment.content %></p>
                                                                <% } %>
                                                                <% if (comment.gif_url) { %>
                                                                    <div class="mb-2">
                                                                        <img src="<%= comment.gif_url %>" alt="GIF" class="img-fluid rounded" style="max-height: 200px;">
                                                                    </div>
                                                                <% } %>
                                                                <small class="text-muted">
                                                                    <%= new Date(comment.created_at).toLocaleString() %>
                                                                </small>
                                                            </div>
                                                            <div>
                                                                <% if (comment.user_id === locals.user?.id || locals.user?.role === 'admin') { %>
                                                                    <div class="d-flex gap-2">
                                                                        <button type="button" class="btn btn-sm btn-primary" data-bs-toggle="modal" data-bs-target="#editCommentModal<%= comment.id %>">
                                                                            <span data-i18n="edit">Edit</span>
                                                                        </button>
                                                                        <form action="/comment/delete/<%= comment.id %>?next=/" method="POST" class="d-inline" onsubmit="return confirm(translate('deleteCommentConfirmation'))">
                                                                            <button type="submit" class="btn btn-sm btn-danger"><span data-i18n="delete">Delete</span></button>
                                                                        </form>
                                                                    </div>
                                                                <% } %>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            <% }); %>
                                        </div>
                                    </div>
                                <% } %>
                                
                                <% if (locals.user) { %>
                                    <form action="/comment" method="POST" class="mt-3">
                                        <input type="hidden" name="itemId" value="<%= item.id %>">
                                        <div class="form-group mb-2">
                                            <textarea name="content" class="form-control comment-text-input" rows="3" placeholder="Add a comment..." data-i18n-placeholder="commentPlaceholder" data-item-id="<%= item.id %>"></textarea>
                                        </div>
                                        
                                        <!-- GIF Search for Comments -->
                                        <div class="mb-3">
                                            <div class="input-group mb-2">
                                                <input type="text" class="form-control comment-gif-search" placeholder="Search for GIFs..." data-i18n-placeholder="searchGif" data-item-id="<%= item.id %>">
                                                <button class="btn btn-outline-secondary search-gif-btn" type="button" data-item-id="<%= item.id %>" data-i18n="searchBtn">Search</button>
                                            </div>
                                            <div class="comment-gif-results row mt-1 mb-1" style="max-height: 250px; overflow-y: auto;" data-item-id="<%= item.id %>"></div>
                                            <input type="hidden" name="gifUrl" class="selected-gif-url" data-item-id="<%= item.id %>">
                                            <div class="selected-gif mt-2 d-none" data-item-id="<%= item.id %>">
                                                <div class="card">
                                                    <div class="card-body">
                                                        <img class="selected-gif-preview img-fluid" data-item-id="<%= item.id %>" src="" alt="Selected GIF">
                                                        <button type="button" class="btn btn-sm btn-danger mt-2 remove-gif-btn" data-item-id="<%= item.id %>" data-i18n="removeGif">Remove GIF</button>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        
                                        <button type="submit" class="btn btn-primary comment-submit-btn" data-item-id="<%= item.id %>" data-i18n="postComment">Post Comment</button>
                                    </form>
                                <% } else { %>
                                    <div class="mt-3">
                                        <div class="alert alert-info">
                                            <i class="bi bi-info-circle"></i> <span data-i18n="loginToComment">Please</span> <a href="/login" data-i18n="login">log in</a> <span data-i18n="or">or</span> <a href="/register" data-i18n="register">create an account</a> <span data-i18n="toPostAndComment">to comment and post.</span>
                                        </div>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                        
                        <!-- Edit Post Modal -->
                        <% if (locals.user && (locals.user.id === item.user_id || locals.user.role === 'admin')) { %>
                            <div class="modal fade" id="editPostModal<%= item.id %>" tabindex="-1" aria-labelledby="editPostModalLabel<%= item.id %>" aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h5 class="modal-title" id="editPostModalLabel<%= item.id %>" data-i18n="editPost">Edit Post</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                        </div>
                                        <form action="/post/edit/<%= item.id %>" method="POST">
                                            <div class="modal-body">
                                                <div class="mb-3">
                                                    <label for="postTitle<%= item.id %>" class="form-label" data-i18n="title">Title</label>
                                                    <input type="text" class="form-control" id="postTitle<%= item.id %>" name="title" value="<%= item.title %>" required>
                                                </div>
                                                <div class="mb-3">
                                                    <label for="postContent<%= item.id %>" class="form-label" data-i18n="content">Content</label>
                                                    <textarea class="form-control" id="postContent<%= item.id %>" name="content" rows="5" required><%= item.content %></textarea>
                                                </div>
                                                <% if (item.gif_url) { %>
                                                    <div class="mb-3">
                                                        <label class="form-label" data-i18n="currentGif">Current GIF</label>
                                                        <div>
                                                            <img src="<%= item.gif_url %>" alt="Current GIF" class="img-fluid rounded" style="max-height: 200px;">
                                                        </div>
                                                        <div class="form-check mt-2">
                                                            <input class="form-check-input" type="checkbox" id="keepGif<%= item.id %>" name="keepGif" value="true" checked>
                                                            <label class="form-check-label" for="keepGif<%= item.id %>" data-i18n="keepGif">
                                                                Keep this GIF
                                                            </label>
                                                        </div>
                                                    </div>
                                                <% } %>
                                            </div>
                                            <div class="modal-footer">
                                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" data-i18n="cancel">Cancel</button>
                                                <button type="submit" class="btn btn-primary" data-i18n="saveChanges">Save Changes</button>
                                            </div>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        <% } %>
                        
                        <!-- Edit Comment Modals -->
                        <% item.comments.forEach(comment => { %>
                            <% if (locals.user && (locals.user.id === comment.user_id || locals.user.role === 'admin')) { %>
                                <div class="modal fade" id="editCommentModal<%= comment.id %>" tabindex="-1" aria-labelledby="editCommentModalLabel<%= comment.id %>" aria-hidden="true">
                                    <div class="modal-dialog">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                                <h5 class="modal-title" id="editCommentModalLabel<%= comment.id %>" data-i18n="editComment">Edit Comment</h5>
                                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                            </div>
                                            <form action="/comment/edit/<%= comment.id %>" method="POST">
                                                <div class="modal-body">
                                                    <div class="mb-3">
                                                        <label for="commentContent<%= comment.id %>" class="form-label" data-i18n="commentContent">Comment Content</label>
                                                        <textarea class="form-control" id="commentContent<%= comment.id %>" name="content" rows="3" required><%= comment.content %></textarea>
                                                    </div>
                                                    <% if (comment.gif_url) { %>
                                                        <div class="mb-3">
                                                            <label class="form-label" data-i18n="currentGif">Current GIF</label>
                                                            <div>
                                                                <img src="<%= comment.gif_url %>" alt="Current GIF" class="img-fluid rounded" style="max-height: 200px;">
                                                            </div>
                                                            <div class="form-check mt-2">
                                                                <input class="form-check-input" type="checkbox" id="keepGif<%= comment.id %>" name="keepGif" value="true" checked>
                                                                <label class="form-check-label" for="keepGif<%= comment.id %>" data-i18n="keepGif">
                                                                    Keep this GIF
                                                                </label>
                                                            </div>
                                                        </div>
                                                    <% } %>
                                                </div>
                                                <div class="modal-footer">
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" data-i18n="cancel">Cancel</button>
                                                    <button type="submit" class="btn btn-primary" data-i18n="saveChanges">Save Changes</button>
                                                </div>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            <% } %>
                        <% }); %>
                    </div>
                <% } %>
            <% }); %>
        </div>
        
        <div class="col-md-6">
            <% items.forEach(function(item, index) { %>
                <% if (index % 2 === 1) { %>
                    <div class="mb-4">
                        <div class="card">
                            <div class="card-body">
                                <div class="d-flex align-items-center mb-3">
                                    <% if (item.author_picture) { %>
                                        <img src="<%= item.author_picture %>" alt="Author" class="profile-picture me-2">
                                    <% } else { %>
                                        <div class="profile-picture-placeholder me-2">
                                            <span><%= item.author_display_name ? item.author_display_name.charAt(0).toUpperCase() : 'U' %></span>
                                        </div>
                                    <% } %>
                                    <div>
                                        <h5 class="card-title mb-0"><%= item.title %></h5>
                                        <small class="text-muted"><span data-i18n="postedBy">Posted by</span> <%= item.author_display_name ? `${item.author_display_name} (@${item.author_username})` : item.author_username %> <span data-i18n="postedOn">on</span> <%= new Date(item.created_at).toLocaleString() %></small>
                                    </div>
                                </div>
                                
                                <p class="card-text"><%= item.content %></p>
                                
                                <% if (item.gif_url) { %>
                                    <div class="mb-3">
                                        <img src="<%= item.gif_url %>" alt="GIF" class="img-fluid rounded" style="max-height: 300px;">
                                    </div>
                                <% } %>
                                
                                <div class="d-flex justify-content-between align-items-center mb-3">
                                    <% if (locals.user) { %>
                                        <form action="/vote/<%= item.id %>?type=upvote" method="POST" class="d-inline">
                                            <button type="submit" class="btn btn-sm <%= item.hasVoted ? 'btn-primary' : 'btn-outline-primary' %>">
                                                <i class="bi bi-hand-thumbs-up<%= item.hasVoted ? '-fill' : '' %>"></i>
                                                <% if (item.hasVoted) { %>
                                                    <span data-i18n="unvote">Unvote</span>
                                                <% } else { %>
                                                    <span data-i18n="vote">Vote</span>
                                                <% } %>
                                            </button>
                                        </form>
                                        
                                        <% if (locals.user.id === item.user_id || locals.user.role === 'admin') { %>
                                            <div class="d-flex gap-2">
                                                <button type="button" class="btn btn-sm btn-primary" data-bs-toggle="modal" data-bs-target="#editPostModal<%= item.id %>">
                                                    <span data-i18n="edit">Edit Post</span>
                                                </button>
                                                <form action="/post/delete/<%= item.id %>" method="POST" class="d-inline" onsubmit="return confirm(translate('deleteConfirmation'))">
                                                    <button type="submit" class="btn btn-sm btn-danger"><span data-i18n="deletePost">Delete Post</span></button>
                                                </form>
                                            </div>
                                        <% } %>
                                    <% } %>
                                    <div class="d-flex gap-2">
                                        <span class="badge bg-primary"><span data-i18n="votes">Votes</span>: <%= item.vote_count %></span>
                                        <span class="badge bg-secondary"><span data-i18n="comments">Comments</span>: <%= item.comments.length %></span>
                                    </div>
                                </div>
                                
                                <% if (item.comments.length > 0) { %>
                                    <div class="mt-3">
                                        <button class="btn btn-sm btn-outline-secondary mb-2 toggle-comments-btn" data-item-id="<%= item.id %>">
                                            <i class="bi bi-chat-square-text"></i> <span data-i18n="showHideComments">Show/Hide Comments</span> (<%= item.comments.length %>)
                                        </button>
                                        <div class="comments-container" data-item-id="<%= item.id %>">
                                            <h6 class="text-muted mb-2"><span data-i18n="comments">Comments</span>:</h6>
                                            <% item.comments.forEach(comment => { %>
                                                <div class="card comment-card mb-2">
                                                    <div class="card-body">
                                                        <div class="d-flex justify-content-between align-items-start">
                                                            <div>
                                                                <div class="d-flex align-items-center mb-2">
                                                                    <% if (comment.commenter_picture) { %>
                                                                        <img src="<%= comment.commenter_picture %>" alt="Commenter" class="profile-picture profile-picture-sm me-2">
                                                                    <% } else { %>
                                                                        <div class="profile-picture-placeholder profile-picture-sm me-2">
                                                                            <span><%= comment.commenter_display_name ? comment.commenter_display_name.charAt(0).toUpperCase() : 'U' %></span>
                                                                        </div>
                                                                    <% } %>
                                                                    <h6 class="card-subtitle mb-1 text-muted">
                                                                        <%= comment.commenter_display_name ? `${comment.commenter_display_name} (@${comment.commenter_username})` : comment.commenter_username %>
                                                                    </h6>
                                                                </div>
                                                                <% if (comment.content && comment.content.trim() !== '') { %>
                                                                    <p class="card-text"><%= comment.content %></p>
                                                                <% } %>
                                                                <% if (comment.gif_url) { %>
                                                                    <div class="mb-2">
                                                                        <img src="<%= comment.gif_url %>" alt="GIF" class="img-fluid rounded" style="max-height: 200px;">
                                                                    </div>
                                                                <% } %>
                                                                <small class="text-muted">
                                                                    <%= new Date(comment.created_at).toLocaleString() %>
                                                                </small>
                                                            </div>
                                                            <div>
                                                                <% if (comment.user_id === locals.user?.id || locals.user?.role === 'admin') { %>
                                                                    <div class="d-flex gap-2">
                                                                        <button type="button" class="btn btn-sm btn-primary" data-bs-toggle="modal" data-bs-target="#editCommentModal<%= comment.id %>">
                                                                            <span data-i18n="edit">Edit</span>
                                                                        </button>
                                                                        <form action="/comment/delete/<%= comment.id %>?next=/" method="POST" class="d-inline" onsubmit="return confirm(translate('deleteCommentConfirmation'))">
                                                                            <button type="submit" class="btn btn-sm btn-danger"><span data-i18n="delete">Delete</span></button>
                                                                        </form>
                                                                    </div>
                                                                <% } %>
                                                            </div>
                                                        </div>
                                                    </div>
                                                </div>
                                            <% }); %>
                                        </div>
                                    </div>
                                <% } %>
                                
                                <% if (locals.user) { %>
                                    <form action="/comment" method="POST" class="mt-3">
                                        <input type="hidden" name="itemId" value="<%= item.id %>">
                                        <div class="form-group mb-2">
                                            <textarea name="content" class="form-control comment-text-input" rows="3" placeholder="Add a comment..." data-i18n-placeholder="commentPlaceholder" data-item-id="<%= item.id %>"></textarea>
                                        </div>
                                        
                                        <!-- GIF Search for Comments -->
                                        <div class="mb-3">
                                            <div class="input-group mb-2">
                                                <input type="text" class="form-control comment-gif-search" placeholder="Search for GIFs..." data-i18n-placeholder="searchGif" data-item-id="<%= item.id %>">
                                                <button class="btn btn-outline-secondary search-gif-btn" type="button" data-item-id="<%= item.id %>" data-i18n="searchBtn">Search</button>
                                            </div>
                                            <div class="comment-gif-results row mt-1 mb-1" style="max-height: 250px; overflow-y: auto;" data-item-id="<%= item.id %>"></div>
                                            <input type="hidden" name="gifUrl" class="selected-gif-url" data-item-id="<%= item.id %>">
                                            <div class="selected-gif mt-2 d-none" data-item-id="<%= item.id %>">
                                                <div class="card">
                                                    <div class="card-body">
                                                        <img class="selected-gif-preview img-fluid" data-item-id="<%= item.id %>" src="" alt="Selected GIF">
                                                        <button type="button" class="btn btn-sm btn-danger mt-2 remove-gif-btn" data-item-id="<%= item.id %>" data-i18n="removeGif">Remove GIF</button>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                        
                                        <button type="submit" class="btn btn-primary comment-submit-btn" data-item-id="<%= item.id %>" data-i18n="postComment">Post Comment</button>
                                    </form>
                                <% } else { %>
                                    <div class="mt-3">
                                        <div class="alert alert-info">
                                            <i class="bi bi-info-circle"></i> <span data-i18n="loginToComment">Please</span> <a href="/login" data-i18n="login">log in</a> <span data-i18n="or">or</span> <a href="/register" data-i18n="register">create an account</a> <span data-i18n="toPostAndComment">to comment and post.</span>
                                        </div>
                                    </div>
                                <% } %>
                            </div>
                        </div>
                        
                        <!-- Edit Post Modal -->
                        <% if (locals.user && (locals.user.id === item.user_id || locals.user.role === 'admin')) { %>
                            <div class="modal fade" id="editPostModal<%= item.id %>" tabindex="-1" aria-labelledby="editPostModalLabel<%= item.id %>" aria-hidden="true">
                                <div class="modal-dialog">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <h5 class="modal-title" id="editPostModalLabel<%= item.id %>" data-i18n="editPost">Edit Post</h5>
                                            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                        </div>
                                        <form action="/post/edit/<%= item.id %>" method="POST">
                                            <div class="modal-body">
                                                <div class="mb-3">
                                                    <label for="postTitle<%= item.id %>" class="form-label" data-i18n="title">Title</label>
                                                    <input type="text" class="form-control" id="postTitle<%= item.id %>" name="title" value="<%= item.title %>" required>
                                                </div>
                                                <div class="mb-3">
                                                    <label for="postContent<%= item.id %>" class="form-label" data-i18n="content">Content</label>
                                                    <textarea class="form-control" id="postContent<%= item.id %>" name="content" rows="5" required><%= item.content %></textarea>
                                                </div>
                                                <% if (item.gif_url) { %>
                                                    <div class="mb-3">
                                                        <label class="form-label" data-i18n="currentGif">Current GIF</label>
                                                        <div>
                                                            <img src="<%= item.gif_url %>" alt="Current GIF" class="img-fluid rounded" style="max-height: 200px;">
                                                        </div>
                                                        <div class="form-check mt-2">
                                                            <input class="form-check-input" type="checkbox" id="keepGif<%= item.id %>" name="keepGif" value="true" checked>
                                                            <label class="form-check-label" for="keepGif<%= item.id %>" data-i18n="keepGif">
                                                                Keep this GIF
                                                            </label>
                                                        </div>
                                                    </div>
                                                <% } %>
                                            </div>
                                            <div class="modal-footer">
                                                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" data-i18n="cancel">Cancel</button>
                                                <button type="submit" class="btn btn-primary" data-i18n="saveChanges">Save Changes</button>
                                            </div>
                                        </form>
                                    </div>
                                </div>
                            </div>
                        <% } %>
                        
                        <!-- Edit Comment Modals -->
                        <% item.comments.forEach(comment => { %>
                            <% if (locals.user && (locals.user.id === comment.user_id || locals.user.role === 'admin')) { %>
                                <div class="modal fade" id="editCommentModal<%= comment.id %>" tabindex="-1" aria-labelledby="editCommentModalLabel<%= comment.id %>" aria-hidden="true">
                                    <div class="modal-dialog">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                                <h5 class="modal-title" id="editCommentModalLabel<%= comment.id %>" data-i18n="editComment">Edit Comment</h5>
                                                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                                            </div>
                                            <form action="/comment/edit/<%= comment.id %>" method="POST">
                                                <div class="modal-body">
                                                    <div class="mb-3">
                                                        <label for="commentContent<%= comment.id %>" class="form-label" data-i18n="commentContent">Comment Content</label>
                                                        <textarea class="form-control" id="commentContent<%= comment.id %>" name="content" rows="3" required><%= comment.content %></textarea>
                                                    </div>
                                                    <% if (comment.gif_url) { %>
                                                        <div class="mb-3">
                                                            <label class="form-label" data-i18n="currentGif">Current GIF</label>
                                                            <div>
                                                                <img src="<%= comment.gif_url %>" alt="Current GIF" class="img-fluid rounded" style="max-height: 200px;">
                                                            </div>
                                                            <div class="form-check mt-2">
                                                                <input class="form-check-input" type="checkbox" id="keepGif<%= comment.id %>" name="keepGif" value="true" checked>
                                                                <label class="form-check-label" for="keepGif<%= comment.id %>" data-i18n="keepGif">
                                                                    Keep this GIF
                                                                </label>
                                                            </div>
                                                        </div>
                                                    <% } %>
                                                </div>
                                                <div class="modal-footer">
                                                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" data-i18n="cancel">Cancel</button>
                                                    <button type="submit" class="btn btn-primary" data-i18n="saveChanges">Save Changes</button>
                                                </div>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            <% } %>
                        <% }); %>
                    </div>
                <% } %>
            <% }); %>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Toggle comments functionality
        const toggleButtons = document.querySelectorAll('.toggle-comments-btn');
        toggleButtons.forEach(button => {
            const itemId = button.getAttribute('data-item-id');
            const commentsContainer = document.querySelector(`.comments-container[data-item-id="${itemId}"]`);
            
            // Hide comments by default
            commentsContainer.style.display = 'none';
            
            button.addEventListener('click', function() {
                if (commentsContainer.style.display === 'none') {
                    commentsContainer.style.display = 'block';
                } else {
                    commentsContainer.style.display = 'none';
                }
            });
        });
        
        // Form validation to allow GIF-only comments
        const commentForms = document.querySelectorAll('form[action="/comment"]');
        commentForms.forEach(form => {
            form.addEventListener('submit', function(e) {
                const itemId = this.querySelector('input[name="itemId"]').value;
                const contentTextarea = this.querySelector(`.comment-text-input[data-item-id="${itemId}"]`);
                const gifUrlInput = this.querySelector(`.selected-gif-url[data-item-id="${itemId}"]`);
                
                // Allow submission if either text or GIF is provided
                if ((contentTextarea.value.trim() === '' || !contentTextarea.value) && !gifUrlInput.value) {
                    e.preventDefault();
                    alert(translate('commentRequiredContent'));
                }
            });
        });

        // Handle comment GIF search
        const searchInputs = document.querySelectorAll('.comment-gif-search');
        const searchBtns = document.querySelectorAll('.search-gif-btn');
        const resultContainers = document.querySelectorAll('.comment-gif-results');
        const selectedGifUrls = document.querySelectorAll('.selected-gif-url');
        const selectedGifs = document.querySelectorAll('.selected-gif');
        const selectedGifPreviews = document.querySelectorAll('.selected-gif-preview');
        const removeGifBtns = document.querySelectorAll('.remove-gif-btn');
        
        // Add event listeners for search buttons
        searchBtns.forEach(btn => {
            btn.addEventListener('click', async function() {
                const itemId = this.getAttribute('data-item-id');
                const searchInput = document.querySelector(`.comment-gif-search[data-item-id="${itemId}"]`);
                const resultsContainer = document.querySelector(`.comment-gif-results[data-item-id="${itemId}"]`);
                
                const searchTerm = searchInput.value.trim();
                if (!searchTerm) return;
                
                try {
                    resultsContainer.innerHTML = '<div class="col-12 text-center"><div class="spinner-border" role="status"><span class="visually-hidden">Loading...</span></div></div>';
                    
                    const response = await fetch(`/api/search-gifs?query=${encodeURIComponent(searchTerm)}`);
                    const data = await response.json();
                    
                    if (data.results && data.results.length > 0) {
                        resultsContainer.innerHTML = '';
                        data.results.forEach(gif => {
                            const gifUrl = gif.media_formats.gif.url;
                            const previewUrl = gif.media_formats.tinygif.url;
                            
                            const col = document.createElement('div');
                            col.className = 'col-md-4 col-6 mb-2';
                            col.innerHTML = `
                                <div class="card gif-card" style="cursor: pointer;">
                                    <img src="${previewUrl}" class="card-img-top" alt="GIF" loading="lazy" style="height: 80px; object-fit: cover;">
                                </div>
                            `;
                            
                            col.querySelector('.gif-card').addEventListener('click', function() {
                                const selectedGifUrl = document.querySelector(`.selected-gif-url[data-item-id="${itemId}"]`);
                                const selectedGif = document.querySelector(`.selected-gif[data-item-id="${itemId}"]`);
                                const selectedGifPreview = document.querySelector(`.selected-gif-preview[data-item-id="${itemId}"]`);
                                
                                selectedGifUrl.value = gifUrl;
                                selectedGifPreview.src = gifUrl;
                                selectedGif.classList.remove('d-none');
                                resultsContainer.innerHTML = '';
                                searchInput.value = '';
                            });
                            
                            resultsContainer.appendChild(col);
                        });
                    } else {
                        resultsContainer.innerHTML = `<div class="col-12"><p class="text-center">${translate('noGifsFound')}</p></div>`;
                    }
                } catch (error) {
                    console.error('Error searching for GIFs:', error);
                    resultsContainer.innerHTML = `<div class="col-12"><p class="text-center text-danger">${translate('gifSearchError')}</p></div>`;
                }
            });
        });
        
        // Add event listeners for remove GIF buttons
        removeGifBtns.forEach(btn => {
            btn.addEventListener('click', function() {
                const itemId = this.getAttribute('data-item-id');
                const selectedGifUrl = document.querySelector(`.selected-gif-url[data-item-id="${itemId}"]`);
                const selectedGif = document.querySelector(`.selected-gif[data-item-id="${itemId}"]`);
                const selectedGifPreview = document.querySelector(`.selected-gif-preview[data-item-id="${itemId}"]`);
                
                selectedGifUrl.value = '';
                selectedGifPreview.src = '';
                selectedGif.classList.add('d-none');
            });
        });
        
        // Allow searching by pressing Enter
        searchInputs.forEach(input => {
            input.addEventListener('keypress', function(e) {
                if (e.key === 'Enter') {
                    e.preventDefault();
                    const itemId = this.getAttribute('data-item-id');
                    const searchBtn = document.querySelector(`.search-gif-btn[data-item-id="${itemId}"]`);
                    searchBtn.click();
                }
            });
        });
    });
</script>
